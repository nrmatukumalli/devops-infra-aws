#!/usr/bin/bash

python3 --version; echo
jq --version; echo
make --version; echo
curl --version; echo
bash --version; echo
pip freeze; echo
docker --version; echo
ssh -V; echo
openssl version; echo
zip --version; echo